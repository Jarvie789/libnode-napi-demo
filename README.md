* Win environment local debugging multiple Visval Studio specified versions build-tools set environment variables.

```
Set npm_config_msvs_version=2019
```

* CMAKE must be in the environment variable
